import Container from "./Container";
import SectionTitle from "./SectionTitle";

const MusicVideo = () => {
  return (
    <Container>
      <SectionTitle className="mt-8 mb-6">Music video</SectionTitle>
    </Container>
  );
};

export default MusicVideo;
